openapi: 3.0.0
info:
  title: Movie API
  description: RESTful API for movie lovers that want to be able to access information about different movies, genres, directors...
  version: 1.0.0
servers:
  - url: https://movie-pool.onrender.com
    description: TODO Optional server description
paths:
  /users:
    get:
      summary: Returns a list of users.
      description: TODO Optional extended description in CommonMark or HTML.
      responses:
        '200':    # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /movies:
    get:
      summary: Returns a list of movies.
      description: TODO Optional extended description in CommonMark or HTML.
      responses:
        '200':    # status code
          description: A JSON array of movies
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string